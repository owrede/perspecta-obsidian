{
  "name": "perspecta-obsidian",
  "version": "0.1.30",
  "description": "Context-switching plugin that saves and restores window arrangements for knowledge work",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "npm run changelog && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "changelog": "node scripts/generate-changelog.mjs",
    "changelog:check": "node scripts/generate-changelog.mjs --check",
    "version": "npm run changelog && node version-bump.mjs && git add CHANGELOG.md manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "context-switching",
    "window-management",
    "workspace"
  ],
  "author": "Perspecta Contributors",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.3.0"
  }
}
